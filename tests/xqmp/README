This directory contains a test code to check DWF buffer allocation etc.
It's neighbor table test, it checks the geometry generated by the initialized,
 and could be run on a pc to check the tables on any node in the cluster.

Makefile     -- makefile
qmp-check.c  -- the main and related functions
qmp.c        -- faux QMP to simulate a single node in a cluster.

